# Ignore repository contents by default. Patterns are matched against the
# destination names produced by ChezMoi, not the `dot_` source names.
*
!.bashrc
!.gitconfig
!.XCompose
!.npmrc
!.config/
!.config/**
